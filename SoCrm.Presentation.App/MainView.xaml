<UserControl x:Class="SoCrm.Presentation.App.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <Menu IsMainMenu="True">
                <MenuItem Header="Security">
                    <MenuItem Header="View Users" Command="{Binding NavigateToUserListCommand}" />
                    <MenuItem Header="Create User" Command="{Binding NavigateToCreateUserCommand}" />
                </MenuItem>
            </Menu>

            <ToolBarTray>
                <ToolBar>
                    <Button Content="w00t" />
                </ToolBar>
            </ToolBarTray>
            <ContentControl Content="{Binding CurrentView}" d:LayoutOverrides="Width, Height"  />
        </StackPanel>
    </Grid>
</UserControl>
